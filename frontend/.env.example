# n8n webhook trigger URL
N8N_WEBHOOK_URL=http://localhost:5678/webhook/contagion-trigger

# This app's public URL (used by n8n to callback)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional: webhook auth token (add to n8n HTTP Request nodes if needed)
WEBHOOK_SECRET=
